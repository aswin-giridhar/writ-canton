import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge otherwise renders into captured deck frames.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
